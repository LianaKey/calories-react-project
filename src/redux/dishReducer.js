export function dishReducer(state, action){
    switch (action.type) {
        case 'ADD_TO_CALC': {
            return 
        }
        case 'REMOVE_TO_CALC': {
            return 
        }
        default:
            return state
    }
}